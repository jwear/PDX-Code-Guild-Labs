from card import Card
from card import Deck
from hand import Hand

def user(user_game):
    user_game = input('Would you like to start a new game - yes or no; or quit?').lower()
    if user_game == 'yes':
    
    else:
        quit()
